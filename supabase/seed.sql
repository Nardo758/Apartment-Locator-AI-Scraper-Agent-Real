-- Minimal seed to satisfy local resets
-- Insert your deterministic dev/test data here

-- Example: create a health check table if not present (safe guard)
-- Note: avoid DDL here; prefer schema in migrations. This file is for data.
-- INSERT INTO public.health_check(default) VALUES (DEFAULT) ON CONFLICT DO NOTHING;
